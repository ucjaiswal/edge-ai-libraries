# Shared collector-signals PVC used by pipeline-manager and vss-collector.
{{- if and (.Values.global.usePvc) (.Values.vsscollector.enabled) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.global.sharedPvcName | default "vss-pvc" }}
  {{- if .Values.global.keepPvc }}
  annotations:
    helm.sh/resource-policy: keep   # Prevent Helm from deleting PVC on uninstall
  {{- end }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.sharedClaimSize | default "10Gi" }}
{{- end }}
